<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.project">

    <uses-permission android:name="android.permission.CALL_PHONE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Project">
        <activity
            android:name=".views.GameInfoActivity"
            android:exported="false" />
        <activity
            android:name=".views.CreateGameActivity"
            android:exported="false" />
        <activity
            android:name=".views.PlayersListActivity"
            android:exported="false" />
        <activity
            android:name=".views.CreateTeamActivity"
            android:exported="true" />
        <activity
            android:name=".views.TeamActivity"
            android:exported="true" />
        <activity
            android:name=".views.AccountActivity"
            android:exported="true" />
        <activity
            android:name=".views.CreateAccountActivity"
            android:exported="true" />
        <activity
            android:name=".views.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>